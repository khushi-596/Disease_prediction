services:
  - type: web
    name: indradhanu-backend
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.10.0

  - type: web
    name: indradhanu-frontend
    env: static
    staticPublishPath: frontend
